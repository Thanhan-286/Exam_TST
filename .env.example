# Supabase — dùng SESSION POOLER (IPv4). Direct connection là IPv6-only.
# Dashboard → Connect → Session pooler
DATABASE_URL=https://tgvmeoytnaxggvnrirtw.supabase.co

# Mật khẩu cho role pbi_reader (Power BI dùng role này, KHÔNG dùng postgres)
PBI_READER_PASSWORD=Nguyenthanhan@286
